// variabel const bernama name menyimpan data string "polinema"
const name = 'Polinema';
//  variabel const dioutputkan dengan alert 
alert(name);

// variabel const digunakan untuk menyimpan data yang bersifat tetap(statis)
// contohnya const port = "8080"
// jika port = "3030", baris ini akan error karena nilai const berubah