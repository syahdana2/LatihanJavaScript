if (true) {
    let name = 'Polinema';
    name = 'Politeknik Negeri Malang';
    alert(name);
}

// sama seperti variabel const, variabel let juga untuk menyimpan data
// tetapi nilai dari variabel let dapat diubah secara dinamis